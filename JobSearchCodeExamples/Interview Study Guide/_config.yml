plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

include:
  - ".NET"
  - ".NET Backend Developer"

theme: null

defaults:
  - scope:
      path: ""
    values:
      layout: default
